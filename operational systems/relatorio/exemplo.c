#include <stdio.h>

int main (int argc, char *argv[])
{
   int i ;                           // uma variavel local

   for (i=0; i< 100; i++)            // um laco for
      printf ("i vale %d\n", i) ;    // uma saida na tela
}
