<% if is_admin? %>
<%= form_for(@addresses_user) do |f| %>
  <% if @addresses_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@addresses_user.errors.count, "error") %> prohibited this addresses_user from being saved:</h2>

      <ul>
      <% @addresses_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :address_id %><br>
    <%= f.text_field :address_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% else %>
<p1> Permission Denied </p1>
<% link_to 'index' %>
<% end %>


